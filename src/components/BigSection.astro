---
const { id, class: customClass } = Astro.props;
---

<section id={id} class="w-full mx-auto max-w-7xl my-24">
    <div
        class={`mx-auto w-full flex flex-col md:flex-row mt-10 ${customClass || ""}`}
    >
        <slot class="mx-auto" />
    </div>
</section>
